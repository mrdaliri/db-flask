Create virtualenv then use following command to install required packages:

```
pip install -r requirements.txt
```

To start server, run `run.py` using **python** interpreter:

```
python run.py
```
